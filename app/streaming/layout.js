export const metadata = {
  title: "Cartel — Streaming",
  description:
    "Cartel DAIEGO para suscripciones de plataformas de streaming y música",
};

export default function StreamingLayout({ children }) {
  return children;
}
